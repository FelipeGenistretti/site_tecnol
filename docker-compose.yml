version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Site_tecnol
    volumes:
      - ./:/var/www
    ports:
      - "8001:8001"
    command: php artisan serve --host=0.0.0.0 --port=8001
